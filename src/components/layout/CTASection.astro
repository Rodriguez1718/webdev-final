---
// CTA Section Component - Professional call-to-action with form
// Placed above footer on all pages
import Button from "../ui/Button.astro";

export interface Props {
  title?: string;
  subtitle?: string;
  description?: string;
  eyebrow?: string;
}

const {
  title = "Ready to Transform Your Business?",
  subtitle = "Get started today and see how we can help you grow",
  description = "Our team of experts is ready to help you build a powerful online presence that drives results.",
  eyebrow = "LET'S GET STARTED",
} = Astro.props;
---

<<<<<<< HEAD
<section class="py-20 bg-white">
  <div class="max-w-7xl mx-auto px-8">
=======
<section class="py-20 md:py-32 px-4 md:px-6 lg:px-8 bg-white">
  <div class="max-w-7xl mx-auto">
>>>>>>> origin/main
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16 items-center">
      <!-- Left Content -->
      <div class="space-y-6">
        <!-- Eyebrow -->
        <div>
          <span
            class="text-sm font-semibold tracking-widest uppercase"
            style="color: var(--helios-orange);"
          >
            {eyebrow}
          </span>
        </div>

        <!-- Main Heading -->
        <div class="space-y-4">
          <h2
            class="text-4xl md:text-5xl font-bold text-[#0A1931] leading-tight"
          >
            {title}
          </h2>
          <p class="text-lg text-[#7E7E7E] leading-relaxed max-w-xl">
            {description}
          </p>
        </div>

        <!-- Features List -->
        <div class="space-y-4 pt-4">
          <div class="flex items-start gap-4">
            <div
              class="flex-shrink-0 w-10 h-10 rounded-full flex items-center justify-center"
              style="background-color: var(--helios-orange);"
            >
              <svg
                class="w-5 h-5 text-white"
                fill="currentColor"
                viewBox="0 0 20 20"
              >
                <path
                  fill-rule="evenodd"
                  d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"
                  clip-rule="evenodd"></path>
              </svg>
            </div>
            <div>
              <h3 class="font-semibold text-[#0A1931] mb-1">Expert Team</h3>
              <p class="text-sm text-[#7E7E7E]">
                Experienced professionals dedicated to your success
              </p>
            </div>
          </div>

          <div class="flex items-start gap-4">
            <div
              class="flex-shrink-0 w-10 h-10 rounded-full flex items-center justify-center"
              style="background-color: var(--helios-orange);"
            >
              <svg
                class="w-5 h-5 text-white"
                fill="currentColor"
                viewBox="0 0 20 20"
              >
                <path
                  fill-rule="evenodd"
                  d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"
                  clip-rule="evenodd"></path>
              </svg>
            </div>
            <div>
              <h3 class="font-semibold text-[#0A1931] mb-1">Proven Results</h3>
              <p class="text-sm text-[#7E7E7E]">
                Trusted by 2,500+ contractors and service providers
              </p>
            </div>
          </div>

          <div class="flex items-start gap-4">
            <div
              class="flex-shrink-0 w-10 h-10 rounded-full flex items-center justify-center"
              style="background-color: var(--helios-orange);"
            >
              <svg
                class="w-5 h-5 text-white"
                fill="currentColor"
                viewBox="0 0 20 20"
              >
                <path
                  fill-rule="evenodd"
                  d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"
                  clip-rule="evenodd"></path>
              </svg>
            </div>
            <div>
              <h3 class="font-semibold text-[#0A1931] mb-1">24/7 Support</h3>
              <p class="text-sm text-[#7E7E7E]">
                Always here to help you succeed
              </p>
            </div>
          </div>
        </div>
      </div>

      <!-- Right CTA Card -->
      <div
        class="bg-gradient-to-br from-[#0A1931] to-[#1a2a4a] rounded-2xl p-8 md:p-10 shadow-xl"
      >
        <h3 class="text-2xl md:text-3xl font-bold text-white mb-2">
          Book a free consultation
        </h3>
        <p class="text-[#99B0BB] mb-8 text-sm">
          Let's discuss how we can help grow your business
        </p>

        <!-- Contact Form -->
        <form class="space-y-4">
          <div>
            <input
              type="text"
              placeholder="Your Name"
              class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-white placeholder-white/50 focus:outline-none focus:border-[var(--helios-orange)] transition-colors duration-200"
              required
            />
          </div>

          <div>
            <input
              type="email"
              placeholder="Your Email"
              class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-white placeholder-white/50 focus:outline-none focus:border-[var(--helios-orange)] transition-colors duration-200"
              required
            />
          </div>

          <div>
            <input
              type="tel"
              placeholder="Your Phone Number"
              class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-white placeholder-white/50 focus:outline-none focus:border-[var(--helios-orange)] transition-colors duration-200"
              required
            />
          </div>

          <div>
            <select
              class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-white focus:outline-none focus:border-[var(--helios-orange)] transition-colors duration-200"
              required
            >
              <option value="" disabled selected class="text-[#0A1931]"
                >Select Your Trade</option
              >
              <option value="hvac" class="text-[#0A1931]">HVAC</option>
              <option value="plumbing" class="text-[#0A1931]">Plumbing</option>
            </select>
          </div>

          <div>
            <textarea
              placeholder="Tell us about your project"
              rows="4"
              class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-white placeholder-white/50 focus:outline-none focus:border-[var(--helios-orange)] transition-colors duration-200 resize-none"
            ></textarea>
          </div>

          <Button variant="primary" fullWidth>
            Get Your Free Consultation
          </Button>
        </form>

        <!-- Trust Badge -->
        <div class="mt-6 pt-6 border-t border-white/10">
          <p class="text-xs text-white/60 text-center">
            ✓ No credit card required • ✓ Free estimate • ✓ No obligation
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  input::placeholder,
  textarea::placeholder,
  select {
    color: rgba(255, 255, 255, 0.5);
  }

  select option {
    background-color: #0a1931;
    color: white;
  }

  input:focus,
  textarea:focus,
  select:focus {
    box-shadow: 0 0 0 3px rgba(244, 140, 6, 0.1);
  }
</style>
